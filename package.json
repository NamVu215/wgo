{
  "name": "wgo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WGo – đi đâu, ăn gì ở Việt Nam. Bắt đầu với Huế.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tests/",
    "kiem-tra": "node scripts/kiem-tra-du-lieu.ts",
    "deploy": "npm run kiem-tra -- --ci && npm run build && wrangler pages deploy dist --project-name wgo --branch main",
    "noi-sheet": "node scripts/noi-google-sheet.ts"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
